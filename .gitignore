# dependencies (bun install)
node_modules

# build output
out
dist
*.tgz
.next

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches and local tooling
.eslintcache
.cache
.source
*.tsbuildinfo
.turbo

# IDE and OS files
.idea
.DS_Store

# generated nested lockfiles (the root lockfile is canonical)
apps/docs/bun.lock

# Type-system performance fixtures are regenerated by the benchmark runner.
benchmarks/type-system/generated/

# TypeScript emits declarations for the shared test-only scanner during package builds.
scripts/scan-module-specifiers.d.ts
scripts/scan-module-specifiers.d.ts.map
