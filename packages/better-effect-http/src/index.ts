/**
 * The public HTTP client API is introduced by the downstream roadmap issues.
 *
 * Keep this entrypoint intentionally empty until those contracts have an
 * implementation. The package boundary is established here so later issues
 * can add APIs without coupling the better-effect core to HTTP.
 */
export {}
